@model CutoverManager.Application.DTOs.ExecutorDTO
@{
    ViewData["Title"] = "Editar Executor";
}

<div class="card shadow-sm">
    <div class="card-header bg-info text-white">
        <h4><i class="fas fa-edit"></i> Editar Executor</h4>
    </div>

    <div class="card-body">

        <form method="post">

            <input type="hidden" asp-for="Id" />

            <div class="form-group">
                <label>Nome *</label>
                <input asp-for="Nome" class="form-control" />
                <span asp-validation-for="Nome" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label>Email</label>
                <input asp-for="Email" class="form-control" />
            </div>

            <div class="form-group">
                <label>Área *</label>
                <select asp-for="IdArea" class="form-control"
                        asp-items="@(new SelectList(ViewBag.Areas, "Id", "Nome"))">
                </select>
            </div>

            <button class="btn btn-info">
                <i class="fas fa-save"></i> Atualizar
            </button>

            <a href="/Executor">Voltar</a>

        </form>

    </div>
</div>
